module.exports = function container (get, set, clear) {
  /*
  program
      .command('run')
      .description('4. run main zenbot daemon')
      .option('--trade', 'do actual trades')
      .option('--tweet_big_trades', 'live tweet big trades')
      .option('--tweet_reports', 'tweet hourly reports')
      .option('-s, --sim', 'run a simulation on backfilled/recorded trades')
      .action(function (options) {
        zenbot('run', options)
      })
  */
  return null
}