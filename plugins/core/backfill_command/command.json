{
  "name": "backfill",
  "spec": "backfill",
  "description": "backfill data using exchange APIs",
  "options": [
    {
      "name": "limit",
      "spec": "-l, --limit <limit>",
      "description": "query limit for API requests",
      "number": true,
      "default": 1500
    }
  ],
  "action": "backfill_trades"
}